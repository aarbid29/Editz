export interface Video {
  id: string;
  title: string;
  description: string;
  publicId: string;
  originalSize: number;
  compressedSize: number;
  // duration: number;
  createdAt: Date;
  updatedAt: Date;
}

export interface Videos {
  id: string;
  title: string;
  description: string;
  publicId: string;
  originalSize: number;
  compressedSize: number;
  // duration: number;
  createdAt: Date;
  updatedAt: Date;
}
